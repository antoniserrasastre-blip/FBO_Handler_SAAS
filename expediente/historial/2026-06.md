# Historial de trabajo — Junio 2026

## 2026-06-02

### Sesión 1 — Adopción del sistema lostandlucky

- Implementado el sistema de carpetas para agentes (filosofía lostandlucky).
- Creada estructura `expediente/` con hojas de dominio: modelo-datos, flujos, ui, api, parsers, infraestructura.
- Reescrito `CLAUDE.md` como router corto (orientación + reglas, sin datos de dominio).
- Corregido: el despliegue es servidor propio `sirvici` expuesto vía Cloudflare Tunnel en `fbo.randomite.space` (no Railway).

### Sesión 2 — Limpieza de .md desfasados

Eliminados (info falsa o concepto sustituido):
- `RAILWAY.md` — no usamos Railway, documentaba un despliegue inexistente.
- `docs/handoff-latest.md` — concepto sustituido por `expediente/historial/AAAA-MM.md`.
- `docs/REFACTOR-V2.md` — la migración está completada; era histórico que ya no aporta.
- `docs/PLAN_TESTEO_PROFUNDO.md` — pendientes vivos extraídos a `estado.md`, registro de fases completadas movido a `historial/2026-05-testeo-profundo.md`.
- `CHANGELOG.md` — registro de versiones obsoleto.
- `ROADMAP.md` — planificación especulativa, choca con la filosofía "a demanda, sin automatismos". Los cambios los marca el usuario en cada sesión y el estado vivo los recoge.

Actualizados:
- `README.md` — corregido el stack de deploy (sirvici + Cloudflare), apunta a `expediente/INDICE.md` como mapa para agentes.
- `expediente/INDICE.md` — añadidas secciones de Historial y ADRs.
- `expediente/estado.md` — pendientes reales del plan de testeo (A6, FASE 4 AENA, FASE 5 PII).

Mantenidos intactos:
- `docs/adr/0001-eventbus-single-container.md` — ADRs son el formato correcto para decisiones permanentes.
- READMEs de microservicios (`aena-microservice/`, `pdf-microservice/`) — específicos de cada servicio.
- `.agents/skills/**`, `.claude/agents/**` — infraestructura de Claude.
- `src/app/prototype/compact-grid/NOTES.md` — vive con su código, se borra cuando se pliega el prototipo.

### Sesión 3 — Revisión del sistema, reconstrucción desde código y skills

**Hecho:**
- **Revisado y mergeado el PR #59** (sistema `expediente/`) tras corregir imprecisiones verificadas contra el código: cifras de test (eran 803/40/0-skip, reales 1004/53-skip/68) y una nota falsa que decía que `upsert.ts` pisa ediciones manuales (lo protege `MOVEMENT_OPERATIONAL_FIELDS` en `upsertMovement()`).
- **Reconstruido TODO el expediente desde el código** (5 agentes en paralelo, código = fuente de verdad). Discrepancias gordas corregidas: enums inventados (`Service` CLEANING/STAIRS/ASU), `COORDINADOR`→`COORDINATOR`, jerarquía de FKs, `api.md` cubría ~28 de ~70 handlers e inventaba un guard "HANDLER" (es `requireWriter`), `db/*` usan `SETUP_SECRET` no NextAuth, `FlightCard` es huérfano (la viva es `VisitCard`), e `infraestructura.md` decía "git pull + PM2, sin CI/CD" cuando es Docker vía GitHub Actions self-hosted con auto-deploy.
- **Corregida la Regla de Oro #2 de `CLAUDE.md`** y los punteros de `INDICE.md`: el punto de entrada del parser PDF es la fachada `pdfParser.ts` (con SAFE_MODE), no `pdfParserV2.ts`.
- **`paths-ignore` en el workflow de deploy** (`**.md`, `docs/**`, `expediente/**`…): los cambios de solo-docs ya NO disparan `docker build --no-cache`. Hace sostenible mantener el expediente al día.
- **Sistema de skills** (`.claude/skills/`): creadas `verificar-expediente`, `actualizar-expediente`, `cerrar-dia`, `nueva-feature`, `auditar-deuda`. Complementan los 6 agentes `fbo-*` sin duplicarlos. `INDICE.md` gana sección "Capacidades". `FILOSOFIA.md` actualizada a la convención `skills/`.
- Analizada la deuda FASE 5 (A3/D2/whitelist) — hallazgos refinados en `estado.md`.

**Decidido:**
- El `expediente/` (en el repo) es la fuente de verdad del dominio; la memoria global se reduce a un puntero.
- Convención dura: **referenciar por símbolo, nunca por número de línea**.
- Whitelist PATCH: no generarla en runtime (frontera de seguridad auditable); mejor un test de paridad.
- GenDec aparcado (decisión de alcance MVP).

**Roto / pendiente:**
- Nada roto. Deploy verde en `d18a54a`, prod sana.
- Sin implementar: deuda FASE 5 (D2 helper, A3 gender/nationality, test de paridad whitelist), A6, FASE 4 AENA.
- Idea futura: un **hook** (`Stop`/`PostToolUse`) que dispare `cerrar-dia`/`actualizar-expediente` de forma determinista en vez de confiar en la invocación del modelo.

### Sesión 4 — Tarjeta compacta enriquecida + fix de estado en pernoctas

**Hecho:**
- **`CompactFlightGrid`**: cada tarjeta muestra ahora, según la dirección del leg de hoy, **pasaje y tripulación** en la fila de la hora (sin crecer de alto; `paxArrivalReal ?? paxArrival` y homólogos de salida — real del handler si existe, si no el estimado del import) y la **ciudad del aeropuerto en lenguaje natural** bajo el ICAO (truncada con tooltip). Todo el dato ya viajaba en el `FlightView`; cero cambios de backend/API. `0165bbd`.
- **Fix de raíz del estado del visit** (`e175681`): `flightView` tomaba `state` del movimiento de salida verbatim (`primary = dep || arr`), así que una pernocta en su día de salida (llegada `PARKED`, salida `EXPECTED`) se mostraba como "Esperando llegada" y la auto-transición se atascaba. Ahora el estado del visit se **compone** como el punto más avanzado de ambos legs (`mostAdvancedState`). Arregla Lista, `/dia` y `VisitCard`, incluido datos ya importados. +4 tests de regresión.
- **Expediente sincronizado** (`08dbcb2`): `ui.md`, `modelo-datos.md`, `flujos.md` (nueva subsección "Estado del visit"), `estado.md` (1004→1008).

**Decidido:**
- **NO tocar `resolveImportState`**: que la salida fuera `PARKED` ya fue un bug corregido a propósito (BUG-2). El `state` per-movimiento es el progreso honesto de cada leg; **componer el estado del visit es responsabilidad de `flightView`**, no del import. El fix vive solo en la capa de derivación.
- Pax/crew en la fila de la hora (no fila propia) para no crecer la tarjeta; ciudad sí, en sublínea diminuta.

**Roto / pendiente:**
- Nada roto. Deploy verde en `0165bbd` (verify + Build&Deploy OK, healthcheck pasado), prod sana. Suite 1008/53-skip.
- Detectado warning lint preexistente: `isOvernight` sin usar en `src/app/api/import/route.ts` (anotado en bugs menores; no bloquea).
- Sigue sin implementar: deuda FASE 5 (D2 helper, A3 gender/nationality, test paridad whitelist), A6, FASE 4 AENA.

### Sesión 5 — Equipaje de bodega: hojas de control + etiquetas por bulto

**Hecho:**
- **Hojas de control de equipaje** (`5323890`, `d440926`): `GET /api/export/baggage-sheets?date=YYYY-MM-DD` → PDF A4, 4 hojitas/página a una cara con líneas de corte, una por salida con `paxCount>0` (estimado del import). Template `src/lib/pdfTemplates/baggageControl.ts`. Réplica de la plantilla Excel real de Mallorcair (descargada de Drive; logo extraído de `xl/media/image1.jpeg` → `public/branding/mallorcair-logo.jpeg`). Botón "Hojas de equipaje" en `/dia` y en el menú Exportar de la Lista (`HomeActionBar`, desktop + móvil).
- **Etiquetas por bulto** (`1087e67`): `GET /api/export/baggage-labels` (stateless por query params) → PDF 62×29 mm, 1 etiqueta por bulto numerada `i/N` (= TOTAL BULTOS de la hoja). Template `baggageLabels.ts`. Componente `PrintLabelsButton` (popover con nº de bultos sugerido=pax, editable) en `FlightDetailPanel` (`/dia`) y `VisitCard` (Lista, al expandir). Para impresoras Brother TD-4650TNWB (203 dpi) / TD-4750TNWB (300 dpi) vía driver del SO.
- Verificado renderizando a PNG con `pdftoppm`: maqueta, logo, numeración i/N y corte correctos.

**Decidido:**
- **Datos desde la BD del Handler, no del Excel** — las hojas dejan de depender de la plantilla con `#NAME?`.
- **Arquitectura de impresión**: el server está FUERA de la oficina → no alcanza la LAN de las impresoras, así que ZPL directo desde el server queda descartado. MVP = PDF a medida + driver Brother (cero infra). Siguiente paso (pendiente de IPs/IT): mini-agente local que reenvíe ZPL al puerto 9100.
- **Etiquetas stateless por params** (no por movementId): la vista de detalle ya tiene el dato fiable y evita acoplarse al mapeo de ids; funciona igual desde `/dia` y la Lista.
- **No guardar el nº de bultos en BD** (decisión del dueño): la hoja se rellena a boli; las etiquetas solo se imprimen.
- Cantidad de etiquetas: **sugerida (pax) y editable** en el momento.

**Roto / pendiente:**
- Nada roto. 3 deploys verdes (verify + Build&Deploy + healthcheck), último `1087e67`. Suite 1008/53-skip, tsc + lint limpios.
- **Gotcha pdfkit documentado**: generar el buffer de forma síncrona da 0 bytes (emite async) → usar Promise + evento `end`. Helvetica no tiene glifos `→` ni `✂` → dibujados como vectores.
- **Expediente sin sincronizar aún**: `api.md` (faltan los 2 endpoints nuevos) y `ui.md` (falta `PrintLabelsButton`, botones en `/dia` y `HomeActionBar`). Sugerido correr `/actualizar-expediente`.
- Sin escribir aún el generador ZPL (a la espera de decisión + IPs).

## 2026-06-03 — FASE 5 / A3: dejar de loguear valor de gender/nationality

**Hecho:**
- **A3 cerrado** (deuda FASE 5, PII en `EventLog.action`). `gender`/`nationality` en `src/app/api/passengers/[id]/route.ts` ya no escriben el valor literal en `action` ni en el `detail` del evento SSE (ambos comen del mismo `changes[]`) → pasan a "gender actualizado" / "nationality actualizada".
- Detectada y arreglada **la misma fuga de `nationality` en `src/app/api/crew/[id]/route.ts`** (línea 46), que NO estaba en el análisis FASE 5 original.
- Blindado con 2 tests de regresión en `src/app/api/pii-audit-log.test.ts` (passenger gender+nationality; crew nationality) — fallarían si vuelve a colarse el valor.
- Suite 1010/53-skip (antes 1008), `tsc --noEmit` limpio. Commit `b35592c`.

**Decidido:**
- Ampliar el alcance de A3 a crew, no solo passengers: misma categoría sensible RGPD, mismo patrón de fuga.

**Roto / pendiente:**
- Nada roto. Commit `b35592c` **sin empujar** (local). FBO despliega por GitHub Actions runner, no por push directo a prod.
- FASE 5 sigue con **D2** pendiente (prioritario): extraer `applyAutoTransition(visitId, session)` para unificar `flights/[id]/route.ts` y `services/[id]/route.ts` y corregir la divergencia de eventos SSE.

## 2026-06-11 — QA run multi-agente + fix run: 26 bugs confirmados y arreglados (sin desplegar)

**Hecho:**
- **QA run** (54 agentes): 4 sondas de tests + 4 causa-raíz con la evidencia de un bot QA que navegó
  producción + verificación adversarial → **26 bugs confirmados, 20 reclamos refutados**. Informe
  completo en La Bestia: `workspace/coding/projects/fbo-handler-saas/qa-run_11-06-2026.md`.
- **Fix run** (9 agentes: 4 fixes con propiedad exclusiva de ficheros + integrador + 2 reviewers +
  remediación): 26/26 fixes aplicados al working tree sobre `74ddad3`. Suite **1137/0/53-skip**
  (desde 1010), tsc + lint + build verdes. Lo gordo:
  - **B1 fantasmas**: filtro de arrastre en GET `/api/flights` + estado terminal **NO_SHOW** +
    sweep al cierre del import (limpiará 484 no-shows históricos al desplegar) + gate de evidencia
    en el reimport (no avanza a PARKED sin ata/livePhase).
  - **Falsos rojos**: fuel pendiente = `REQUESTED` (no `!== SERVED`; 1260/1302 movements eran
    NOT_REQUESTED) vía helper compartido `hasPendingDepServices` (fila + cabecera, toilet incluido).
  - **ATA/ATD resucitadas**: eventLogs por fin serializados en la API (desc, take 20), regex
    aceptan labels históricos ("En calzos") y códigos nuevos, ATA = primera transición (mis-click).
  - **D2 cerrado**: `src/lib/autoTransition.ts` unifica flights/services con SSE idéntico y log
    con código de estado.
  - **B3 contadores**: `src/lib/movementCounts.ts` único criterio (solo-fecha) en cabecera, banners,
    timeline e histórico ("X lleg · Y sal"); DaySummary sobre estados reales.
  - **B6/B2b/B4/B7**: orden por leg propio en zonas, SALIDAS exige etd, alertas sin suelo -30 min
    (persisten hasta atd), callsigns dobles en el panel.
  - **Horas**: calcMinutes acepta `HHMM`; `normalizeTime` en pdfParserV2 con ParseWarning (A6 parcial).
- Review adversarial cazó 1 hallazgo alto (fallback de movementCounts contaba legs inexistentes de
  quick-adds solo-salida) → remediado: contar exige evidencia de que el leg existe.

**Decidido:**
- **NO_SHOW** como estado terminal (no en FLIGHT_STATES para no romper los Record exhaustivos).
- Las 4 sondas `*.probe.test.ts` quedan como regresión (verdes); las refutadas reescritas
  documentando el contrato real con comentario.
- **Live tracking**: migrar de OpenSky (caído: anónimo sin créditos, basic auth retirado) a
  **adsb.lol** + polling adaptativo + estado FINAL + push ntfy — tras desplegar esta fix run.
  Medio plazo: receptor ADS-B propio en el FBO.

**Roto / pendiente:**
- Nada roto. **TODO SIN COMMITEAR**: 28 ficheros modificados + 13 nuevos en el working tree,
  pendiente de revisión del dueño y push (= deploy). Suite verde verificada dos veces.
- Decisiones de producto abiertas: etiquetado de rotaciones dobles (B2), urgencia en FlightChip,
  unificar isArrivalToday de diaHelpers con movementCounts.
