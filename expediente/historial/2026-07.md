# Historial de trabajo — Julio 2026

## 2026-07-17

### Piloto escribano día 1 + decisión spike MCP

- **Agente escribano en turno real (día 1):** Claude operó la app en vivo durante el turno de
  Toni vía API (usuario `claude` rol HANDLER, PATCH `/api/flights`): 65 vuelos importados,
  ~35 escrituras, 7 incidencias (solo en chat — no hay campo), report de turno generado desde
  la BD y enviado. Flujo y reglas: `workspace/personal/routines/fbo-escribano.md`.
- **Decisión de dirección (La Bestia):** desarrollo agente-operable → **MCP spike-demo acotado**
  (MCP in-app + modelo Incident + tokens AGENT); multi-tenant sigue en nevera. Ficha:
  `workspace/direccion/fichas/fbo-mcp-spike-demo_17-07-2026.md`.

## 2026-07-18

### Piloto escribano día 2 + bugfix dobles rotaciones

- **Día 2 del piloto:** 2 imports (57+54 filas) con cancels selectivos, ~50 escrituras PATCH,
  ~10 incidencias solo-en-chat, report enviado el mismo día. Fricción del día 1 corroborada;
  concepto del spike completo y alcance cerrado con Toni
  (`workspace/.../stages/01_concept/output/spike-mcp-concepto_17-07-2026.md`).
- **Bugfix dobles rotaciones (desplegado en verde):** `upsertVisit` keyeaba por avión+palmaDay
  y la 2ª rotación del día pisaba a la 1ª (caso D-ASIM, detectado en el piloto). Commits
  `06fdbb9` (matching por callsign + hora ±90 min, con exclusión por pasada de import) y
  `75df350` (eliminado `@@unique(aircraftId, palmaDay)`; migrado en schema,
  `migrate-v2-schema.mjs` y `/api/db/migrate`; drift test invertido). Suite 1147 pass.
  D-ASIM reparado en producción vía API — la reparación verificó el fix e2e.
- Pendiente que deja: migrar el target Vercel/Turso (el unique sigue vivo allí) — lo tiene Toni.

## 2026-07-19

### Auditoría ICM (desde La Bestia) — higiene aplicada

- Auditoría de estructura repo + nodo workspace (read-only, informe en
  `workspace/.../references/_archivo/auditoria-icm_19-07-2026.md`). Veredicto: el expediente
  centralizado se queda como está (no se impone CONTEXT.md-por-carpeta).
- **Borrados los 4 componentes huérfanos legacy** (~1.900 líneas, 0 imports verificados
  incluyendo tests): `FlightCard.tsx`, `ServiceCheckbox.tsx`, `CrewInventory.tsx`,
  `LiveStatusBadge.tsx`. Marcados "candidata a borrado" en ui.md desde el 02-06.
- Corregido el drift de los READMEs de microservicios ("Vercel-hosted" → self-host en sirvici;
  "future folders" del aena-README ya existían). Nota de auto-transición de `flujos.md` §4
  marcada resuelta (unificada en `autoTransition.ts` el 11-06).
- Deuda anotada en estado.md: god-component `src/app/page.tsx` (extraer `useHomeFilters()`).
- En La Bestia: nodo ICM completo montado (stages + KB de dominio, fichas matching-rotaciones
  y reglas-TZ) — siguiente paso `/design-sprints` del spike MCP.
